let username = window.prompt("What's your name?");    // Receives user input by assigning the value entered to a variable via a windows prompt

console.log(username);    // Displays the value of the variable to the console window